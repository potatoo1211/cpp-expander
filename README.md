```cpp
#include　"..."
```
のようにダブルクォーテーションで囲まれたincludeの中身を再帰的に展開してクリップボードにコピーします。

十中八九既存だけど入れるのめんどくさかったので作りました。

`F5`:実行のみ
`Ctrl+Shift+B`:実行&コピー

```bash
npm run compile
```
をしてできた`cpp-expander-0.0.1.vsix`をダウンロードしたらいけると思います。